pip install gunicorn - WSGI server instead of flask

pip install odfpy - py package for odf sheets

pip install -r requirements.txt  
// r - repository , 

// repository mentioned are Flask==2.3.2 , pandas==2.0.3,pyexce-ods==0.6.0


install executable icon install pyinstaller in project folder
$python -m pip install pyinstaller 
